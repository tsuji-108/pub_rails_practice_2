<% content_for :title, "Editing board" %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h3 mb-0">Board 編集</h1>
  <div class="d-flex gap-2">
    <%= link_to "詳細に戻る", @board, class: "btn btn-outline-secondary" %>
    <%= link_to "一覧に戻る", boards_path, class: "btn btn-outline-secondary" %>
  </div>
</div>

<div class="mb-4">
  <%= render "form", board: @board %>
</div>
