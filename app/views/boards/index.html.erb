<% content_for :title, "Boards" %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h3 mb-0">掲示板一覧</h1>
  <%= link_to "掲示板を作成する", new_board_path, class: "btn btn-primary" %>
</div>

<% if @boards.any? %>
  <div id="boards" class="row row-cols-1 row-cols-md-2 g-4">
    <% @boards.each do |board| %>
      <div class="col">
        <%= render partial: "board", locals: { board: board, show_actions: true } %>
      </div>
    <% end %>
  </div>
<% else %>
  <div class="alert alert-info" role="alert">
    まだボードがありません。最初のボードを作成しましょう。
  </div>
<% end %>
