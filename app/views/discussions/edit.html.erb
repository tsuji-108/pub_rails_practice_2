<% content_for :title, "Editing discussion" %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h3 mb-0">Discussion 編集</h1>
  <div class="d-flex gap-2">
    <%= link_to "詳細に戻る", @discussion, class: "btn btn-outline-secondary" %>
    <%= link_to "一覧に戻る", discussions_path, class: "btn btn-outline-secondary" %>
  </div>
</div>

<div class="mb-4">
  <%= render "form", discussion: @discussion %>
</div>
